//Asignación

// = igual a
// + sumado a
// - restado a
// * multiplicado por
// / dividido entre
// % el resto de (10 % 5 = 0, 10 % 6 = 4)
// ** exponenciado por

//Aritméticos

// -- decremento
// ++ incremento

//Comparación

// == igual a
// === identico a (cabe aclarar que no es lo mismo == que === ya que el tercer = hace que se compare hasta el tipo de valor)
// != no es igual a
// !== no es identico a
// >= mayor o igual que
// > mayor que
// <= menor o igual que
// < menor que

//Lógicos
